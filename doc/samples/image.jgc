#boilerplate

$safe_z = 10
$default_f = 600
$plunge_f = 100

(Image carving)
#set($img = $Shapes.surface.image.file("samples/suzanne.png").size(50,50).height(3).tool("3 ball"))

#g($img.origin("x0y0").strategy("simple 0 alt"))

m2

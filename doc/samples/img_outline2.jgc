#boilerplate
#set($rapidnear = true)

$safe_z = 10
$default_f = 600
$plunge_f = 100

(Demonstrate outline toolpaths)
#set($img = $Shapes.surface.image.size(50,40).height(8).stepover("50%"))

#g($img.file("suzanne.png").tool("3 ball").strategy("rough"))
#g($img.file("suzanne.png").strategy("outline 0.5-3"))

m2
